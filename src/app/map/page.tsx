'use client';

import { MapContainer } from '@/components/blocks/MapContainer/MapContainer';

export default function MapPage() {
  return (
    <main className="bg-gray-25 min-h-screen">
      <MapContainer />
    </main>
  );
}
